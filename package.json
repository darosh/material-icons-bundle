{
  "name": "material-icons-bundle",
  "version": "1.0.0",
  "devDependencies": {
    "axios": "^0.17.0",
    "cpy-cli": "^1.0.1",
    "deploy-to-gh-pages": "^1.3.6",
    "json-stringify-pretty-compact": "^1.0.4",
    "lodash": "^4.17.4",
    "material-design-icons": "^3.0.1",
    "mdi-svg": "^2.0.46",
    "mkdirp": "^0.5.1",
    "recursive-readdir": "^2.2.1",
    "rimraf": "^2.6.2",
    "sharp": "^0.18.4",
    "shelljs": "^0.7.8",
    "xml2js": "^0.4.19"
  },
  "license": "MIT",
  "module": "index.js",
  "scripts": {
    "build": "npm run build:download && npm run build:meta && npm run build:compare && npm run build:bundle && npm run build:unique && npm run build:export",
    "build:bundle": "node build/bundle.js",
    "build:compare": "node build/compare.js",
    "build:download": "node build/download.js",
    "build:export": "node build/export.js",
    "build:meta": "node build/meta.js",
    "build:unique": "node build/unique.js",
    "clean": "rimraf index.js icons meta/*.json",
    "deploy": "cpy meta/*.* !meta/_*.* demo && deploy-to-gh-pages --local demo"
  }
}
